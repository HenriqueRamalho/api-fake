{
  "name": "api-fake",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start:dev": "json-server --watch index.js --port 3000",
    "start:prod": "json-server index.js --port 3000"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "json-server": "^0.17.3"
  },
  "devDependencies": {
    "@faker-js/faker": "^8.0.2"
  }
}
